## Linux 平台 Bug 测试计划

### 已发现的潜在问题（代码审查阶段）

1. **Bug 1 (低)** — `doctor_linux.go:213-216` `currentGroupDetail()` 组检测逻辑反转
2. **Bug 2 (中)** — `provider_linux.go` 热键后端检测仅检查 `XDG_SESSION_TYPE`，但不检查 `WAYLAND_DISPLAY`
3. **Bug 3 (中)** — `autotype_linux.go:95-98` `pasteX11()` 粘贴失败时原剪贴板无法恢复
4. **Bug 4 (低)** — `autotype_linux.go:141` Wayland 恢复剪贴板错误静默忽略
5. **Bug 5 (低)** — `doctor_linux.go` 无 `no_x11` 构建变体
6. **Bug 6 (低)** — `clipboard_linux.go:45-48` Wayland 下不尝试回退 X11 工具

### 测试步骤

1. 运行现有单元测试 (`go test ./... -v`)
2. 不同构建标签编译测试 (`-tags no_x11`)
3. 完整 X11 版本编译 (`CGO_ENABLED=1 go build`)
4. 环境诊断 (`--doctor`)
5. 逐一验证上述 6 个 bug
6. 检查 `CHANGELOG.md` 是否需要更新