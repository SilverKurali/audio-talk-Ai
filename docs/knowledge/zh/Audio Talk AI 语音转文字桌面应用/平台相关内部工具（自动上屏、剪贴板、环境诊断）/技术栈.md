Go 1.21+ 的 `//go:build` 标签分发；macOS 端使用 CGO 链接 `ApplicationServices`（Accessibility、Event Taps）；Windows 端纯 Go 使用 `github.com/atotto/clipboard`（Win32 剪贴板）与 `user32.SendInput`（模拟 Ctrl+V 上屏），无 cgo；Linux 剪贴板同时支持 X11 (`xclip`) 与 Wayland (`wl-clipboard`)，通过 `WAYLAND_DISPLAY`/`XDG_SESSION_TYPE` 环境变量自动选择。