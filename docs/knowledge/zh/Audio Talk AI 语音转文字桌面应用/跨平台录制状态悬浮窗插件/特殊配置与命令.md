Linux 构建需安装 `libwayland-dev`、`libx11-dev`、`libxext-dev`、`libxinerama-dev`、`libxrender-dev`；`no_x11` build tag 会同时禁用 X11 与 Wayland 后端（两者均要求 `linux && !no_x11`），退回 stub 空实现；macOS 需启用 CGO（`//go:build darwin && cgo`）。