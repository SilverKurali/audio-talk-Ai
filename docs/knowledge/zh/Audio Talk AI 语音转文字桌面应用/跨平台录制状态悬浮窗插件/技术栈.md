cgo 直连 libwayland-client（wlr-layer-shell v1 + xdg-shell）、Xlib/Xrender/Xinerama/Shape 扩展；macOS 侧通过 `-framework AppKit -framework Foundation` 调用 Objective-C 辅助进程；Wayland 协议头由 `protocols/*.xml` 经 wayland-scanner 生成。