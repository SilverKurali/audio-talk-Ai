Go + cgo 调用原生系统 API（Windows 除外，纯 Go 无 cgo）：Windows 使用 `golang.org/x/sys/windows` 的 `SetWindowsHookExW`/`GetMessageW`；macOS 通过 CGEventTap + CFRunLoop 捕获事件并用 pipe 传回 Go；Linux X11 直接链接 `-lX11` 并使用 `XGrabKey`；Wayland 走 evdev `/dev/input/event*`（需要 input 组读取权限）。