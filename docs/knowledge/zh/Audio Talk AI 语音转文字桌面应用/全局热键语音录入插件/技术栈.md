Go 1.21+；macOS 使用 CGO 链接 CoreAudio/AudioToolbox（`#cgo LDFLAGS: -framework AudioToolbox -framework CoreAudio`）；Linux 依赖 alsa-utils 的 arecord；Windows 优先 ffmpeg dshow，回退 sox waveaudio；ASR Provider 通过 `asr.NewClient` 工厂 + `asr/drivers` 空白导入触发 init() 注册的自描述元数据模式动态加载。