make build → go build -o ./build/audio-talk-ai ./cmd/audio-talk-ai；make install 先 build，再运行 audio-talk-ai --install 把主程序安装到 ~/.local/bin，并复制仓库内 bin/daudio-talk-ai 可断开会话包装脚本到 ~/.local/bin；make run 直接 go run 当前平台；make test 跑全模块测试；make deps 执行 tidy/download。