Go 1.25 单模块；TOML 配置（BurntSushi/toml）+ AES-256-GCM 凭据加密；fsnotify 配置热重载；Bubble Tea/Lipgloss TUI；gorilla/websocket（讯飞系列）与 coder/websocket（豆包、OpenAI Realtime）用于 ASR 流式协议，WebUI 为纯 net/http + embed；creack/pty 提供可断开终端会话；bendahl/uinput 用于 Wayland 自动上屏；各平台 cgo 后端（XGrabKey、CoreAudio AudioQueue、evdev）。